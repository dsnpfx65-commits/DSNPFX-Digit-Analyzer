const MARKET_ORDER=["1HZ100V","R_100","1HZ75V","R_75","1HZ50V","R_50","1HZ25V","R_25","1HZ10V","R_10"];const MARKET_NAMES={"1HZ100V":"Volatility 100 (1s)","R_100":"Volatility 100","1HZ75V":"Volatility 75 (1s)","R_75":"Volatility 75","1HZ50V":"Volatility 50 (1s)","R_50":"Volatility 50","1HZ25V":"Volatility 25 (1s)","R_25":"Volatility 25","1HZ10V":"Volatility 10 (1s)","R_10":"Volatility 10"};const cards=new Map();let latestMarkets={};const fmtPct=v=>`${Number(v||0).toFixed(2)}%`;const fmtNum=v=>Number(v||0).toFixed(2);const safeText=v=>v===null||v===undefined||v===""?"--":String(v);function renderShell(){const stack=document.getElementById("marketStack"),template=document.getElementById("marketCardTemplate"),select=document.getElementById("botMarket");MARKET_ORDER.forEach(symbol=>{const node=template.content.firstElementChild.cloneNode(true);node.dataset.symbol=symbol;node.querySelector(".market-name").textContent=MARKET_NAMES[symbol];node.querySelector(".market-mode").textContent=symbol.startsWith("1HZ")?"SHADOW":"PRODUCTION";node.querySelector(".market-mode").classList.toggle("shadow",symbol.startsWith("1HZ"));stack.appendChild(node);cards.set(symbol,node);const option=document.createElement("option");option.value=symbol;option.textContent=MARKET_NAMES[symbol];select.appendChild(option);node.querySelector(".use-signal").addEventListener("click",()=>selectSignal(symbol))})}function updateMarket(symbol,market){const card=cards.get(symbol);if(!card)return;const verified=Boolean(market&&market.is_premium&&market.published_prediction!==null&&market.published_prediction!==undefined);card.classList.toggle("signal",verified);card.querySelector(".live-price").textContent=safeText(market?.displayed_price??market?.price);card.querySelector(".last-digit").textContent=safeText(market?.last_digit);card.querySelector(".market-regime").textContent=safeText(market?.regime||"COLLECTING");card.querySelector(".verified-confidence").textContent=fmtPct(market?.calibrated_confidence);card.querySelector(".edge-score").textContent=fmtNum(market?.edge_score);card.querySelector(".model-agreement").textContent=safeText(market?.model_agreement||"0 / 0");card.querySelector(".market-quality").textContent=safeText(market?.market_quality||"LEARNING");card.querySelector(".decision-pill").textContent=verified?"MATCH SIGNAL":safeText(market?.decision||"WAIT");const button=card.querySelector(".use-signal");button.disabled=!verified;const digit=card.querySelector(".scanner-digit"),label=card.querySelector(".scanner-label"),status=card.querySelector(".match-status"),note=card.querySelector(".scanner-note");clearTimeout(card._revealTimer);card.classList.remove("revealed");digit.textContent="--";label.textContent="SCANNING";status.textContent="Analyzing";note.textContent="Checking production evidence";card._revealTimer=setTimeout(()=>{card.classList.add("revealed");if(verified){digit.textContent=market.published_prediction;label.textContent="PREDICTION";status.textContent="Match Found";note.textContent=`Verified confidence ${fmtPct(market.calibrated_confidence)}`}else{digit.textContent="--";label.textContent="WAIT";status.textContent="No Verified Match";note.textContent="NO EDGE / keep scanning"}},1400+MARKET_ORDER.indexOf(symbol)*90)}function selectSignal(symbol){const market=latestMarkets[symbol];if(!market||!market.is_premium)return;document.getElementById("botMarket").value=symbol;document.getElementById("botPrediction").value=market.published_prediction;document.getElementById("selectedPrediction").textContent=market.published_prediction;document.getElementById("selectedMarket").textContent=MARKET_NAMES[symbol]||symbol;document.getElementById("selectedDecision").textContent="MATCH";document.getElementById("selectedConfidence").textContent=fmtPct(market.calibrated_confidence);document.getElementById("selectedEdge").textContent=fmtNum(market.edge_score);document.querySelector('[data-tab="setup"]').click()}function updateStats(stats={}){const pick=(...keys)=>{for(const key of keys)if(stats[key]!==undefined)return stats[key];return 0};document.getElementById("productionWins").textContent=pick("production_wins","wins");document.getElementById("productionLosses").textContent=pick("production_losses","losses");document.getElementById("productionAccuracy").textContent=fmtPct(pick("production_accuracy","accuracy"));document.getElementById("recentAccuracy").textContent=fmtPct(pick("recent_accuracy","last20_accuracy"));document.getElementById("resolvedSignals").textContent=pick("resolved_signals","production_resolved","resolved");document.getElementById("pendingSignals").textContent=pick("pending_signals","pending")}function applyPayload(payload={}){latestMarkets=payload.markets||latestMarkets||{};MARKET_ORDER.forEach(symbol=>updateMarket(symbol,latestMarkets[symbol]||{}));const live=MARKET_ORDER.filter(s=>latestMarkets[s]&&["live","LIVE"].includes(latestMarkets[s].status)).length,verified=MARKET_ORDER.filter(s=>latestMarkets[s]?.is_premium).length;document.getElementById("onlineMarkets").textContent=`${live} / 10`;document.getElementById("verifiedSignals").textContent=verified;document.getElementById("scannerStatus").textContent=safeText(payload.status||"LIVE").toUpperCase();document.getElementById("lastUpdate").textContent=`Updated ${new Date().toLocaleTimeString()}`;updateStats(payload.statistics||{})}async function initialLoad(){try{const response=await fetch("/api/state");if(response.ok){const state=await response.json(),[marketsRes,statsRes]=await Promise.all([fetch("/api/markets"),fetch("/api/statistics")]),marketsData=marketsRes.ok?await marketsRes.json():{},statsData=statsRes.ok?await statsRes.json():{};applyPayload({...state,markets:Object.fromEntries((marketsData.markets||[]).map(m=>[m.symbol,m])),statistics:statsData})}}catch(error){console.warn("Initial dashboard load failed",error)}}function connectWebSocket(){const proto=location.protocol==="https:"?"wss":"ws",socket=new WebSocket(`${proto}://${location.host}/ws`),badge=document.getElementById("connection");socket.addEventListener("open",()=>{badge.classList.add("online");badge.innerHTML="<span></span>LIVE"});socket.addEventListener("message",event=>{try{applyPayload(JSON.parse(event.data))}catch(error){console.warn("Invalid websocket payload",error)}});socket.addEventListener("close",()=>{badge.classList.remove("online");badge.innerHTML="<span></span>RECONNECTING";setTimeout(connectWebSocket,2500)});socket.addEventListener("error",()=>socket.close())}function setupTabs(){document.querySelectorAll(".tab-button").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll(".tab-button").forEach(b=>b.classList.toggle("active",b===button));document.querySelectorAll(".tab-panel").forEach(panel=>panel.classList.toggle("active",panel.id===`tab-${button.dataset.tab}`))}))}function setupStrategy(){document.getElementById("saveStrategy").addEventListener("click",()=>{const strategy={market:document.getElementById("botMarket").value,stake:document.getElementById("stake").value,takeProfit:document.getElementById("takeProfit").value,stopLoss:document.getElementById("stopLoss").value,martingale:document.getElementById("martingale").value,multiplier:document.getElementById("multiplier").value,maxLosses:document.getElementById("maxLosses").value};localStorage.setItem("dsnpfx-bot-strategy",JSON.stringify(strategy));document.getElementById("botMessage").textContent="Strategy saved locally. Live execution remains disabled until a separate execution module is explicitly enabled."})}renderShell();setupTabs();setupStrategy();initialLoad();connectWebSocket();